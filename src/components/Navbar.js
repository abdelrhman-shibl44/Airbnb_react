import logo from "../images/Vector.png"
export const Navbar = () => {
    return (
        <nav className="Navbar">
            <div className="container">
                <a href="#"><img src={logo} /></a>
            </div>
        </nav>
    )
}